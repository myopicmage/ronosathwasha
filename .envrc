# `path:.` reads the working tree. A bare `.` resolves the flake through git,
# so an uncommitted or untracked change to flake.nix is silently ignored and
# the shell you get is the one from the last commit.
use flake "path:."
